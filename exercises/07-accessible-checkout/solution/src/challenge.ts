export type Checkout={name:string,address:string,payment:string};export function validateCheckout(x:Checkout){const e:Record<string,string>={};if(!x.name.trim())e.name="required";if(!x.address.trim())e.address="required";if(!x.payment.trim())e.payment="required";return e}export function buildOrder(x:Checkout){return {customerName:x.name.trim(),address:x.address.trim(),paymentMethod:x.payment}}
