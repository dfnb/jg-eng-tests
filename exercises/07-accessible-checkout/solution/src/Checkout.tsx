import React,{useState} from "react";export function Checkout(){const[busy,setBusy]=useState(false);return <form onSubmit={()=>setBusy(true)}><label htmlFor="name">Name</label><input id="name" name="name"/><label htmlFor="address">Address</label><input id="address" name="address"/><label htmlFor="payment">Payment</label><select id="payment" name="payment"><option>card</option></select><button disabled={busy}>Buy</button><p aria-live="polite">{busy?"Sending":""}</p></form>}
