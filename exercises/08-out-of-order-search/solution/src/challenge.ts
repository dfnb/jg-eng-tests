export class SearchController{private generation=0;private disposed=false;publish:(x:string[])=>void;constructor(publish:(x:string[])=>void){this.publish=publish}async search(term:string,fetcher:(x:string)=>Promise<string[]>){const g=++this.generation;if(!term.trim()){if(!this.disposed)this.publish([]);return}try{const r=await fetcher(term);if(!this.disposed&&g===this.generation)this.publish(r)}catch(e){if(g===this.generation&&!this.disposed)throw e}}dispose(){this.disposed=true;this.generation++}}
