export type Item={id:string,title:string,version:number};export class OptimisticStore{items=new Map<string,Item>();constructor(items:Item[]){items.forEach(x=>this.items.set(x.id,x))}async edit(id:string,title:string,send:(x:Item)=>Promise<Item>){const old=this.items.get(id);if(!old)throw new Error("missing");const next={...old,title};this.items.set(id,next);try{this.items.set(id,await send(next));return"ok"}catch{this.items.set(id,old);return"failed"}}}
